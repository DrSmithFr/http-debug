version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - misspell
    - revive
    - unconvert
    - whitespace
  settings:
    revive:
      rules:
        - name: exported
          disabled: true
  exclusions:
    rules:
      # Test helpers routinely ignore errors that matter in production code.
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt
